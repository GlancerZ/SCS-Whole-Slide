# PyTorch whole-slide training environment. CUDA wheels are installed from the
# matching official PyTorch index; see optimizations/scs_streaming/PYTORCH.md.
h5py>=3.11,<4
numpy>=2.0,<3
scipy>=1.13,<2
torch==2.13.0
